<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/css/main.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <%-include ./partials/header.ejs  %> 
    <h2><%= title %></h2>
    <section class="item-detail-container">
      <h3><%=brand.name%></h3>
      <h4><%=brand.origin%></h4>
      <p class="summary">
        <span>Summary</span>
        <p><%-brand.summary%></p>
      </p>
      <form class="delete-form" method="POST">
        
        <% if (items!="") { %>
            <p>Delete all of the Brand Items before deleting the brand !</p>
            <% } else { %>
                <p>Are you sure you want to delete this item?</p>
                <button type="submit">Delete</button>
        <% } %>
       
      </form>
    </section>
    <section class="browse-container">
        <% if (items=="") { %>
            <h2>There are currently no items by this Brand</h2>
        
            <% } else { %>
                <h2>Browse the Brand Products</h2>
        <%-include ./partials/items_list.ejs%>
        <% } %>
      </section>
  </body>
</html>



